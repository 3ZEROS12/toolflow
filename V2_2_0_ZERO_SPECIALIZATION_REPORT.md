# ToolFlow v2.2.0: 彻底去特化，将意图理解与架构推导还给真实大模型

## 一、 核心痛点与本次重构实质

在之前的版本中，为了实现“Ponytail 偷懒原则”，代码中充斥着机械的正则（如硬过滤某些关键词）以及在发给 LLM 的 Prompt 中下了“死命令”：
`The first option in Slot 1 MUST have id starting with "opt_ecosystem_" and label starting with "[生态协同] 直接调用已安装的 @<tool_name> 插件 (强烈推荐)"`。

这导致大模型被强行绑架：即使用户做的是后端微信连接，本地只有一个毫不相干的 `@pi-btw`，大模型也被迫将其作为强烈推荐呈递给用户，造成严重的“乱推荐”与“产物误判”。

## 二、 真正的架构分工重塑

1. **彻底废除 Prompt 死命令**：
   - 允许大模型根据任务领域客观评估本地工具；
   - 只有存在真实强相关的工具（如自动化测试遇到 playwright）时才推荐生态；无相关插件时堂堂正正推荐标准自研与工程架构，绝不硬蹭。

2. **彻底废除业务正则推导**：
   - 移除所有对业务领域的正则猜测；
   - 新增 `synthesizeBlueprintPlanWithLLM`：在蓝图生成时，调用活跃大模型直接输出 `primaryArtifact`（主产物路径）与 `isFrontend`，将架构推导彻底归还给真正理解语义的大模型。

3. **代码回归底层安全守门员**：
   - 负责 0-Token 真实环境自省；
   - 负责 Kahn DAG 拓扑排序与无环调度；
   - 负责敏感文件物理拦截与 Git 快照回滚；
   - 负责 3 次失败确定性物理熔断。
