# ToolFlow v1.9.0 实战痛点攻坚与架构加固报告

## 针对真实任务测试反馈的四大硬伤深度攻坚

### 1. 彻底清除 `[@生态插件]` 伪徽章与空标签污染
- **问题根因**：`ui.ts` 在渲染选项装配的高阶能力时，对未命名或空生态对象默认兜底字符串 `"生态插件"`。
- **治理效果**：
  - 彻底移除了无意义的硬编码字符串兜底；
  - 增加严格过滤，若当前选项无具体挂载的第三方能力，整行直接隐藏，保持界面清爽纯净；
  - 仅在真实命中已安装能力时，直白展示 `@真实插件名`、`@真实Skill名`。

### 2. 支持 `[Esc]` 步进式回退上一决策维度（Step-Back Navigation）
- **问题根因**：`ui.ts` 的全局按键监听将 `Esc` 直接绑定为 `done(null)` 彻底杀死弹窗，导致用户无法返回修改上一维度的选择。
- **治理效果**：
  - 引入 `currentStepIndex` 决策状态栈；
  - 当处于第 2 步及以后时，按 `Esc` 无缝回退到上一个决策维度（并保留上一维度的用户已选状态）；
  - 仅在第 1 步按 `Esc` 才优雅退出弹窗；
  - 底部提示条新增自适应提示：`[Esc] 返回上一步` / `[Esc] 退出`。

### 3. 会话（Session）生命周期彻底解耦与物理隔离（解决跨 Session 幽灵唤醒）
- **问题根因**：`.pi/blueprint_state.json` 长期驻留磁盘，新开 Session 时被无差别载入，后台 `turn_end` 持续触发旧任务自愈。
- **治理效果**：
  - 监听原生 `session_start` 事件，当检测到全新会话（`reason === "new" | "clear"`）时，物理清空内存残留；
  - 当用户在入口交互中选择 `[新建蓝图]` 时，物理执行 `resetState()`，抹除磁盘持久化文件与旧快照，杜绝幽灵唤醒。

### 4. Stage 1 强制工具调用约束与靶向自愈引导（根治仅思考不写文件导致的 3 次熔断）
- **问题根因**：模型在设计阶段仅输出了长篇思考（Thinking）而未调用 `write` 工具落盘，触发了物理门禁未找到 `design.md` 的熔断。
- **治理效果**：
  - 在 Stage 1 的行动指令中注入强硬指令：`CRITICAL ACTION: You MUST invoke the 'write' tool to create the file '...' now. Do NOT merely discuss or think without writing.`；
  - 自愈提示词增加靶向纠偏反馈：`【重要指引】你刚才仅进行了思考或文字探讨，并未物理写文件！请立即调用 'write' 工具将规范完整写入落盘。`
