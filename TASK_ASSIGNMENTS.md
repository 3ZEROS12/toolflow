# ToolFlow (Workflow Architect) 专项攻坚任务委派与排期表

**发起人**：Jason (ToolFlow 专项攻坚组)  
**执行周期**：2026-09-02 至 2026-09-23  
**状态**：已正式派发，各责任人即刻进入实施阶段  

---

## 一、 团队角色分工与任务派发清单

### 📌 任务 1：AST 语法与类型级别确定性门禁（Phase 1 核心攻坚）
- **责任人**：**Alex (核心架构组)**
- **配合人员**：Devin (测试组)
- **截止时间**：2026-09-08
- **具体实施要求**：
  1. 新增 `ast_gate.ts`：实现基于轻量 AST 解析的物理门禁，支持 TS/JS/JSON/YAML 语法完整性即时快筛。
  2. 扩展 `state.ts` 中的 `verifyStageArtifacts` 逻辑：在原有的 `size > 0` 和 SHA-256 校验之后，增加 AST 语法树无破坏断言。
  3. 新增对应单元测试并合并入 `test_suite.ts`（Module 13）。

---

### 📌 任务 2：Wave 级并发调度引擎与物理沙箱隔离（Phase 2 核心攻坚）
- **责任人**：**Brian (并发与运行时组)**
- **配合人员**：Sarah (底层工具组)
- **截止时间**：2026-09-15
- **具体实施要求**：
  1. 新增 `wave_scheduler.ts`：改造 Kahn DAG 波次执行循环，同 Wave 无依赖 Stage 采用 `Promise.allSettled` 并发执行。
  2. 搭建 Copy-on-Write 临时工作区沙箱：确保并发 Stage 之间文件写入互不冲突，验收通过后原子归并。
  3. 与 `blast_radius.ts` 深度结合，保持并发状态下的写权限锁与审计拦截。

---

### 📌 任务 3：运行时自适应子图重规划与局部自愈（Phase 3 核心攻坚）
- **责任人**：**Elena (算法与状态机组)**
- **配合人员**：Jason (技术把关)
- **截止时间**：2026-09-20
- **具体实施要求**：
  1. 新增 `dynamic_dag.ts`：捕获 Stage 3 次就地自愈失败的堆栈信息，实现 `diagnoseAndReconstruct` 局部重构算法。
  2. 支持在 DAG 中动态剔除故障节点、就地插入 Tier 2 / Tier 3 平替节点，实现自动化死锁逃逸。
  3. 保留 Git 影子快照与回滚兜底保底机制。

---

### 📌 任务 4：MCP 标准化暴露与跨业务仓库压测（Phase 4 核心攻坚）
- **责任人**：**Kevin (生态与外部集成组)**
- **配合人员**：Michael (资源协调)
- **截止时间**：2026-09-23
- **具体实施要求**：
  1. 新增 `mcp_server.ts`：将 ToolFlow 的编排能力封装为标准化 Model Context Protocol 服务。
  2. 接入 React、Rust、Python 3 个真实业务仓库进行端到端全链路自动化压测。
  3. 输出全景压测性能对比与稳定性评估报告。

---

## 二、 协作规范与每日同步机制

1. **分支管理**：各责任人在 `feature/<module_name>` 分支独立开发，提交前必须保证 `npx tsx test_suite.ts` 全绿。
2. **每日 Standup**：每日 10:00 进行 15 分钟站会，同步前一日进度与阻塞点（Blockers）。
3. **物理门禁红线**：任何新增代码必须严格遵循“0 硬编码、0 Token 冗余脱水、100% 物理测试断言”三大铁律。
