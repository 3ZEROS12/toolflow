# ToolFlow ⌬

> Pi 的动态工具沙箱与上下文脱水调度器。  
> 按阶段精准装卸工具，长日志落盘脱水，物理拦截危险写入。

[English](README.md) | [简体中文](README_zh.md)

---

### 30 秒了解

**问**：“我在 Pi 里装了一堆 MCP 和插件，现在模型动不动选错工具，随便跑条命令刷几百行日志，Token 啪一下就满了。”  
**答**：“干活不用把整个工具箱都挂身上。查资料给搜索工具，写代码给编辑工具，走查时撤掉写权限。不需要的工具，连声明都不进上下文。”

**问**：“终端那些长日志怎么处理？”  
**答**：“超过 40 行的输出直接落盘存文件，上下文里只留路径和摘要，省下大几千 Token。”

**问**：“任务做完之后呢？”  
**答**：“原有工具全量归还，不留垃圾。”

**问**：“怎么用？”  
**答**：“输入 `/toolflow 做个新功能`，剩下的它自己处理。”

---

> **定位**：专为挂载多个 MCP/插件的中大型工程设计。普通单文件修补用 Pi 原生即可；长流程、多工具协作时，用 ToolFlow 管控工具与上下文。

---

## 安装方式

通过 git 直接安装：

```bash
pi install git:github.com/3ZEROS12/toolflow
```

---

## 核心价值

### 1. 动态工具沙箱（按阶段裁剪工具链）
ToolFlow 绝不把全量工具一股脑塞给大模型，而是根据执行阶段动态挂载：
- **调研设计阶段**：仅挂载感知与搜索工具（`read`, `grep`, `web_search`），物理禁用代码修改工具，防止模型在尚未调研清楚时草率动刀。
- **核心实现阶段**：仅挂载当前任务需要的核心编辑工具（`write`, `edit`），卸载外部无关检索工具，保持极度专注与低 Token 开销。
- **验收走查阶段**：通过 `ReviewIsolationGuard` 从运行时**物理剥离**代码修改权限，确保审核员处于纯粹客观的“只读/只验证”状态。

### 2. 四重 Token 治理闭环（削减 95% 冗余开销）
ToolFlow 在运行时构筑了四道物理级 Token 节流防线：
- **实时长日志拦截与脱水**：当终端（`bash`/`powershell`）或外部工具产生冗长日志（>40 行或异常堆栈）时，中间件毫秒级将其全量落盘归档（`.pi/toolflow/runs/...`），上下文内仅保留核心头尾指纹与磁盘归档路径，即刻阻断上下文污染。
- **重型工具 JIT 阶段挂载**：借助 LLM 深度推理，重型 MCP 或高成本工具只在真正需要的阶段露头，未激活阶段彻底屏蔽其庞大的 JSON Schema。
- **阶段流转物理脱水契约**：阶段交接时监听 `session_before_compact`，自动剔除历史探索痕迹、试错代码与琐碎心路历程，向后仅传递已落盘的物理产物路径。
- **无感正向回落反馈**：配合 `session_compact`，每次精简压缩均会在状态栏给出轻量级释放提示，让用户实时感知 Token 水位下降，告别爆仓焦虑。

### 3. 工具生命周期严格还原（借还闭环）
首秒自动捕获会话初始工具快照。无论是正常竣工、意外崩溃，还是执行 `/toolflow reset`，都会 100% 将环境恢复到原生完整面貌，保证主会话无任何幽灵残留。

### 4. 物理级爆炸半径护栏（Blast Radius Guard）
在引擎底层同时监控原生写工具与终端执行指令（`bash`, `powershell`）。无论是通过代码编辑，还是试图通过终端脚本重定向覆写/删除核心敏感文件（`.env*`, `.git*`, 核心依赖锁），系统都会从底层直接物理阻断。

### 5. 原生 Prompt 模板工作台
在终端内直接输入 `/toolflow` 呼出提示词工作台：
- **实时探测**：自动汇总本地与全局的 `prompts/*.md`，按最后修改时间排序。
- **一键预填**：按 `[p]` 键将高亮模板一键载入输入栏。
- **就地新建**：按 `[c]` 键直接在终端创建新的提示词模板，支持大模型辅助提炼命令标签。

---

## 常用命令

| 命令 | 说明 |
| :--- | :--- |
| `/toolflow` | 打开 Prompt 模板工作台与任务初始化面板 |
| `/toolflow <任务内容>` | 启动带工具沙箱与阶段治理的受控任务 |
| `/toolflow status` | 查看当前阶段流水线与挂载工具状态 (别名: `/sop`) |
| `/toolflow rollback` | 撤销当前阶段的修改，回退到阶段开始前的快照 |
| `/toolflow-rollback` | `/toolflow rollback` 的独立快捷指令 |
| `/toolflow reset` | 清除当前任务状态并清理脱水临时缓存 |
| `/toolflow export` | 将阶段规划与架构决策导出为 `BLUEPRINT.md` |

---

## 快捷键指南

- `[Enter]`：输入任务并开始受控执行
- `[p]`：将选中的 Prompt 模板填入输入框
- `[c]`：在终端就地新建 Prompt 模板
- `[1-4]` / `[←/→]`：在方案决策面板中切换选项
- `[Esc]`：退出当前浮层

---

## 开源协议

MIT © [Jason](https://github.com/3ZEROS12)
