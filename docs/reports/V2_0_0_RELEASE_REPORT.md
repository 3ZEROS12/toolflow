# ToolFlow v2.0.0 核心漏洞重构修复归档

## 1. 生态相关性语义过滤 (彻底根治乱推荐)
- 严禁抓取无关插件 (如 btw/side-q) 充当主业务能力。
- 引入白名单与负向关键词阻断，严格校验插件实际描述是否与任务强关联。
- 后端/微信服务任务绝不再误判为前端页面 (杜绝生成 index.html)。

## 2. 剪贴板真实粘贴支持 (支持复制粘贴输入)
- 支持 Windows/Linux 终端 Bracketed Paste 协议 (\x1b[200~ ... \x1b[201~)。
- 支持多字符/多字节流一次性安全灌入 inputBuffer，解决无法复用历史任务的问题。

## 3. 架构师灵感切题轮转与循环采纳 (按 s 切换/采纳)
- 支持多灵感按 [s] 键循环轮转 (currentSparkIndex++) 与开关切换。
- 灵感若已在主槽位体现则自动去重，避免千篇一律。

## 4. 彻底取消退出 (Esc 绝不自动按默认方案开工)
- 修复了主流程对 null/cancel 返回值的放任穿透。
- Esc 退出决策舱时彻底终止执行，绝不自动开启后台代理。
