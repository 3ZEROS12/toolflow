# ToolFlow (⌬)

> **Adaptive Tool & Ecosystem Meta-Orchestrator for Pi**  
> 消除全量工具上下文污染，以大白话共创打破人机意图断层，以 Kahn 拓扑分波与严苛物理门禁确保代码真实可运行。

---

## 核心设计哲学与原则

1. **坚持通用元编排，拒绝硬编码特化 (Zero Specialization)**：
   - 彻底废除写死特定业务场景的死板模板与关键词，交付物与架构方案由 LLM 结合工程物理拓扑动态决策；
   - 无论是 Web、后端微服务、中间件、CLI 还是脚本工程，均自适应推导最佳工程入口与验收策略。

2. **去术语化，全流程大白话与启发式共创**：
   - 选项聚焦于「用户能得到什么结果与收益」，消除底层生硬黑话；
   - 标配 **4 大直观维度**：功能形态偏好、视觉风格/交互基调、制作代码结构、**AI 架构师灵感推荐**；
   - 支持随时按 `[e]` 自由补充一句话定制需求，作为最高优先级硬约束执行。

3. **分层工具可见性架构 (Layered Affordance Architecture)**：
   - **L0 基础探针常驻**：`read`, `grep`, `find`, `bash`, `powershell` 全周期开放，彻底杜绝排查排错时失明；
   - **L1/L2 高杠杆工具阶段赋能**：设计阶段赋能联网检索，编写阶段赋能 `workflow`/`subagent` 并发分发，门禁阶段赋能对抗走查。

4. **严苛物理门禁与 3 次就地自愈熔断**：
   - 不接受聊天框里的口头完成，严格以真实落盘文件的存在性（`size > 0` 字节）与构建测试退出码（`ExitCode 0`）为唯一完工依据；
   - 允许前期阶段的合法代码调研与探索（`read`/`ls`/`grep`/`find` 等），严防探索行为误扣重试次数；
   - 阶段启动前自动建立 Git 快照，遇到改崩支持一键秒级回滚。

5. **极简工业美学与量化价值结算**：
   - 纯净苯环 `⌬` 终端美学，严格遵守行宽限制防止终端溢出崩溃；
   - 任务竣工自动输出 `VALUE DELIVERY RECEIPT` 结算单，直观量化交付清单与效率收益。

---

## 安装与使用

### 通过 Pi 安装

```bash
# 从 GitHub 直接安装
pi install git:github.com/3ZEROS12/toolflow

# 或者从 npm 安装（发布后）
pi install npm:toolflow
```

---

## 命令指南

| 命令 | 用途与说明 |
| :--- | :--- |
| `/toolflow` | **无参启动**：查看本地生态全景画像，按 `[Enter]` 原地键入任务目标，按 `[p]` 预填 Prompt 模板 |
| `/toolflow <任务目标>` | **带目标启动**：直接唤出 4 维度大白话共创决策舱，生成自适应执行蓝图 |
| `/toolflow export` (或 `-e`) | **导出蓝图文档**：将当前蓝图的 Kahn DAG 拓扑与契约一键导出至 `BLUEPRINT.md` |
| `/toolflow rollback` (或 `-r`) | **秒级无损回滚**：当阶段代码写坏时，一键撤销变更并恢复纯净工作区 |
| `/toolflow reset` | **重置状态**：彻底清空当前会话执行状态机与物理缓存碎片 |

---

## 终端交互快捷键

- `[Enter]` / `[i]` / `[Space]`：切入任务输入框，开启架构推导
- `[p]`：将光标处高亮的 Prompt 模板预填入输入框
- `[Ctrl+L]`：在 Prompt 模板新建视图中，基于大纲极速生成模板指令与说明
- `[1]` ~ `[4]`：数字键极速选择对应方案
- `[←]` / `[→]`：快速切换选项 A 与选项 B
- `[e]` / `[E]`：原地切入个性化需求自由补充输入框
- `[+]` / `[=]`：为当前维度添加自定义选项
- `[a]` / `[A]`：敏捷秒开（一键采用推荐方案秒级开工）
- `[Esc]`：退出或返回上一步

---

## 开源协议

MIT License.
